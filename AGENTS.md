# Agent rules (product-v1)

Read also: `/home/library/.grok/PERMANENT-MEMORY.md` (host-wide).

## Output & thinking (permanent)
- Think deeply; expose only concise results unless asked for reasoning.
- Brevity is default for wording; never skip investigation, verification, or risk checks to be brief.
- Simple Q: 1–3 short paragraphs.
- Task wrap-ups must include **completion %**, what changed + verification, remaining/blockers, recommendations if needed.
- No filler, motivational text, or repeated caveats.
- Follow-ups only when they change the result.

## Product (locked)
- See `docs/PRODUCT-V1-PLAN.md` / Obsidian `GitAtlas-Parvaana/TRUE-PRODUCT-PLAN.md`.
- Parvaana = content brain; GitAtlas = graph; product = face + orchestration.
